import React from "react";
import "./style.css";

function RouterButton({ text }) {

    return (
        <div className="label">
            { text }
        </div>
    );
}

export default RouterButton;
